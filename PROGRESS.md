# PROGRESS

> CLAUDE.md의 "개발 순서 (Claude Code 작업 단위)" 10단계를 기준으로 진행 상황을 기록합니다.

## 1. 완료된 작업

- ✅ Docker 환경 구성 — `docker-compose.yml`, `frontend/Dockerfile`, `backend/Dockerfile` 작성 (개발용, 볼륨 마운트 기반 핫리로드)
- ✅ Frontend 프로젝트 생성 — Vite + React + TypeScript 스캐폴드 (`frontend/`)
- ✅ Backend 프로젝트 생성 — Express + TypeScript (`backend/src/server.ts`, `tsconfig.json`, `package.json`)
- ✅ Socket.IO 연결 확인 — `backend/src/socket/index.ts`에 연결/해제 핸들러 구현, handshake(`/socket.io/`) 및 `/health` 응답 검증 완료
- ✅ 방 생성 기능 — `room/room.types.ts`, `room.store.ts`(6자리 코드 발급), `room.service.ts`의 `createRoom`, socket `createRoom` → `roomCreated`
- ✅ 방 참가 기능 — `room.service.ts`의 `joinRoom`, socket `joinRoom` → `playerJoined` (방 없음/가득 참 시 `error`)
- ✅ Ready 기능 — `room.service.ts`의 `setReady`/`allReady`, 양측 Ready 시 `gameState`를 `PLAYING`으로 전환 후 `gameStarted` emit
- ✅ 가위바위보 게임 구현 — `game/rps/rps.types.ts`(Hand), `rps.game.ts`(판정), `game/index.ts`(gameType 라우팅), socket `selectHand` → 양측 선택 완료 시 `result` emit
- ✅ 재경기 기능 — `room.service.ts`의 `markRematchReady`, 양측 준비 시 손패/ready 초기화 후 `PLAYING` 재진입, `rematchStarted` emit
- ✅ Frontend 구현 — `types/index.ts`(공유 타입), `hooks/useSocket.ts`(소켓 싱글턴), `pages/MainPage.tsx`(방 생성/참가), `pages/RoomPage.tsx`(대기실/Ready), `pages/GamePage.tsx`(손 선택/결과/재경기), `App.tsx`(화면 전환)
- ✅ 리팩터링 — Vite 기본 스캐폴드 잔재(`App.css`, `assets/`, `public/icons.svg`) 정리, `index.html` 타이틀 변경
- ✅ 통합 테스트 — `docker compose up --build` 기동 후 socket.io-client로 방 생성→참가→Ready→선택→결과→재경기→재판정 전체 플로우 자동 검증 완료 (룸 코드 발급, rock-beats-scissors 판정, paper-paper 무승부 판정 모두 정상)
- ✅ 브라우저 수동 테스트 — 사용자가 직접 브라우저에서 확인, 정상 작동 확인
- ✅ GitHub 저장소 생성 및 연결 — https://github.com/paaye7313/JoinMyGame (Public), 초기 커밋 푸시 완료
- ✅ 버그 수정: 재경기 동의 없이 첫 재경기가 즉시 시작되던 문제 — `room.service.ts`의 `finishRound`가 라운드 종료 시 `player.ready`를 리셋하지 않아, 로비 Ready 단계에서 남아있던 `ready=true`가 그대로 `markRematchReady`의 `allReady()` 판정에 재사용되던 것이 원인. `finishRound`에서 라운드 종료 시 전원 `ready=false`로 초기화하도록 수정. socket.io-client 스크립트로 "한쪽만 재경기 클릭 시 rematchStarted 미발생 / 양쪽 클릭 시 발생" 검증 완료.
- ✅ 버그 수정(부분): 한글 닉네임 입력 중 마지막 글자가 사라지는 문제 — `MainPage.tsx`의 닉네임 `<input>`이 `onChange`(native `input` 이벤트)에만 의존해, IME 조합이 `input` 이벤트 없이 `compositionend`로만 끝나는 경우 React state가 갱신되지 않고, 이후 다른 상태 변화로 리렌더될 때 컨트롤드 인풋이 오래된 state로 DOM을 덮어써 글자가 사라짐. `onCompositionEnd`와 `onBlur`에서 실제 DOM 값을 state에 명시적으로 재동기화하도록 수정 — "같은 페이지 안에서 다른 입력으로 포커스 이동" 케이스는 Playwright로 재현·수정 확인 완료. 단, "브라우저 탭 전환 단축키와 마지막 키 입력이 정확히 겹쳐 그 키 자체가 브라우저 크롬으로 소비되는" 레이스 컨디션은 웹페이지 JS로 근본 해결 불가능한 브라우저/OS IME 레벨 이슈로 확인됨(대형 한국 사이트에서도 동일 현상 재현되는 것을 사용자가 확인) — 이 부분은 프로젝트 코드로 추가 조치하지 않기로 함.
- ✅ `frontend/index.html`의 `<html lang="en">` → `lang="ko"`로 변경 — 크롬이 페이지를 영어로 오인해 새로고침 시 번역 팝업이 뜨던 문제 해결 (IME 버그와는 무관, 별개 개선).
- ✅ 비주얼 리디자인 (파스텔 캐주얼 + 카드 게임 연출): Tailwind CSS v4(`@tailwindcss/vite`) 도입, `index.css`에 라이트/다크 파스텔 컬러 토큰(`--primary`/`--secondary`/`--peach`/`--danger` 등)을 `@theme inline`으로 매핑. 향후 다른 게임 추가를 대비해 `components/ui/`(Button, Card, Badge, PlayerAvatar) 공유 레이어와 `game/rps/`(백엔드 `game/rps/`와 대칭) 전용 레이어로 분리. 가위바위보를 버튼이 아니라 **카드를 내는 형식**으로 재구성: `game/rps/cards.ts`에 `Hand` → 표시 정보(이모지/라벨) 매핑을 별도 데이터 테이블(`CardDef`)로 분리해, 나중에 특수카드를 추가할 때 이 파일만 확장하면 되도록 설계함(단, 이번 작업은 비주얼까지만 — 특수카드 판정 로직/백엔드는 손대지 않음, `types/index.ts`의 `Hand` 유니온도 그대로 유지). `game/rps/components/HandCard.tsx`(카드 한 장, face-up/face-down/revealed) + `PlayZone.tsx`(나·상대 대결 슬롯, 동시 flip 리빌)로 `GamePage.tsx`를 재구성, `RoomPage.tsx`/`MainPage.tsx`도 공유 UI로 재스타일링.
  - 🐛 구현 중 발견: CSS 3D 카드 플립에서 `.flip-card-back`/`.flip-card-front`의 정적 `rotateY(180deg)`를 반대쪽에 걸어서, 애니메이션이 끝나면 오히려 뒷면(미스터리 패턴)이 보이고 실제 카드 앞면이 숨는 버그가 있었음. Playwright로 결과 화면을 캡처했을 때 텍스트가 뜨자마자(트랜지션 시작 직후, 0ms 근처) 스크린샷을 찍으면 우연히 정상처럼 보여서 처음엔 놓쳤고, 리빌 후 700ms~1000ms 대기한 뒤 다시 캡처해서야 발견함 — `rotateY(180deg)`는 "리빌 후에 보여야 하는 면(front)" 쪽에 걸어야 함. 이후 승/패 하이라이트(초록 링/글로우, 패배 카드 흐림)까지 정상 동작 확인.
  - 카드 뒷면 아이콘을 `🂠`(카드 슈트 유니코드 기호, 폰트 지원이 약해 일부 환경에서 tofu 박스로 렌더링됨)에서 `🎴`(컬러 이모지, 호환성 좋음)로 교체.
  - Playwright(`playwright-core` + 로컬 캐시된 Chromium)로 두 브라우저 컨텍스트를 띄워 방 생성→참가→Ready→카드 선택→결과→재경기 전체 플로우와 다크모드를 검증. `npm run build`/`npm run lint` 모두 통과.
  - ⚠️ `frontend/package.json`에 `tailwindcss`, `@tailwindcss/vite`가 새로 추가됨 — Docker 이미지는 `npm install` 시점에 이 의존성을 받으므로, 다음에 `docker compose up`으로 띄울 때는 반드시 `--build`(또는 `docker compose build frontend`)로 이미지를 다시 빌드해야 함. 그냥 `up`만 하면 기존 익명 볼륨의 구 `node_modules`를 그대로 쓰게 되어 Tailwind가 빠진 채로 뜰 수 있음. (실제로 이번 세션에서 `up --build`만으로는 익명 볼륨이 구 `node_modules`를 물고 있어 `@tailwindcss/vite` 모듈을 못 찾는 에러가 났고, `down -v` 후 재기동해서 해결함 — 41번 메모와 동일한 케이스.)
- ✅ Ready/재경기 상대방 상태 실시간 표시: 서버가 `ready` 필드 변경(로비 Ready, 라운드 종료로 인한 리셋, 재경기 동의) 시마다 새 이벤트 `playersUpdated`(`{ players }`)를 방 전체에 브로드캐스트하도록 `backend/src/socket/index.ts`의 `ready`/`selectHand`/`rematch` 핸들러에 추가(각각 `roomService.setReady`/`finishRound`/`markRematchReady` 직후). `RoomPage.tsx`와 `GamePage.tsx`는 이제 로컬 낙관적 상태 대신 서버가 보내주는 `players` 배열의 `ready` 필드를 그대로 신뢰해서, 상대방이 Ready/재경기 버튼을 누르는 즉시 내 화면에도 배지로 표시됨(`RoomPage`는 Ready/대기 중, `GamePage` 결과 화면은 재경기 동의/대기 중). `CLAUDE.md`의 Server → Client 이벤트 표에도 `playersUpdated` 추가함. Playwright로 "A만 Ready/재경기 클릭했을 때 B 화면에 A의 배지가 뜨는지" 직접 검증 완료.
  - 참고: `removePlayer`(disconnect) 쪽은 이번에 손대지 않음 — 상대방이 연결이 끊겨도 남은 플레이어의 화면에 별도 알림/목록 갱신 이벤트가 없는 기존 갭은 그대로 남아있음 (필요해지면 별도로 다룰 것).
- ✅ 상대방 나감 처리: 위에서 남겨둔 갭을 메움. `backend/src/socket/index.ts`의 `disconnect` 핸들러가 `roomService.removePlayer(socket.id)`의 반환값(남은 플레이어가 있는 `Room`)을 보고 새 이벤트 `playerLeft`(`{ players }`)를 방에 브로드캐스트하도록 수정(방이 완전히 비어 삭제된 경우엔 emit 안 함). `removePlayer`는 기존에도 남은 플레이어들의 `ready`/`selectedHand`를 초기화하고 `gameState`를 `WAITING`으로 되돌리고 있었으므로, 프론트만 이 이벤트를 받아 화면을 전환하면 됐음. `App.tsx`가 전역으로 `playerLeft`를 구독해 현재 화면이 `game`이면 `room` 화면(남은 플레이어만 담긴 목록)으로 강제 전환하고, 상단에 "상대방이 방을 나가서 대기실로 돌아왔습니다" 토스트(4초 후 자동 소멸)를 띄움. 이미 `room` 화면에 있을 때(게임 시작 전 로비 단계에서 상대가 나간 경우)는 `RoomPage`가 직접 `playerLeft`를 `playerJoined`/`playersUpdated`와 동일하게 처리해 목록만 갱신(별도 화면 전환 불필요). `CLAUDE.md` 이벤트 표에 `playerLeft` 추가. Playwright로 두 시나리오(게임 중 상대 브라우저 컨텍스트 종료 / 로비 대기 중 상대 종료) 모두 검증 완료 — 게임 중 이탈 시 남은 플레이어가 대기실로 돌아오고 목록에서 상대가 사라짐, 로비 이탈 시에도 목록에서 즉시 사라짐.
  - `나가기`(GamePage 결과 화면 버튼)로 스스로 나가는 경우는 이번 범위 밖 — 그건 서버에 아무 것도 알리지 않고 프론트만 메인 화면으로 돌아가는 기존 동작 그대로임(소켓 연결은 유지된 채 방에 남아있는 상태). 필요하면 별도로 `leaveRoom` 같은 명시적 이벤트를 추가해야 함.
- ✅ 대기실(RoomPage) 나가기 버튼 추가 + 자발적 나가기 이벤트: 위에서 남긴 갭까지 마저 메움. `backend/src/socket/index.ts`에 `leaveCurrentRoom(io, socket)` 헬퍼를 추출해 새 client→server 이벤트 `leaveRoom`(페이로드 없음, `removePlayer`가 socketId만으로 방을 찾음)과 기존 `disconnect` 핸들러가 동일 로직(플레이어 제거 → `socket.leave(roomCode)` → 남은 인원 있으면 `playerLeft` 브로드캐스트)을 공유하도록 정리. `RoomPage.tsx`에 "나가기" 버튼(Ready 버튼 옆, secondary variant) 추가 — 클릭 시 `leaveRoom` emit 후 `onExit()`으로 메인 화면 복귀. `App.tsx`가 `RoomPage`에 `onExit` prop 전달. 겸사겸사 `GamePage.tsx`의 기존 "나가기" 버튼도 동일하게 `leaveRoom`을 emit하도록 고쳐서, 그동안 서버에 알리지 않고 나가던(방에 유령처럼 남아있던) 기존 버그를 같이 잡음. `CLAUDE.md` Client → Server 이벤트 표에 `leaveRoom` 추가. Playwright로 (1) 대기실에서 나가기 클릭 시 본인은 메인 화면으로, 상대방 화면에선 즉시 목록에서 사라짐 (2) 나간 직후 같은 방 코드로 제3의 플레이어가 실제로 입장 가능함(서버 쪽 슬롯이 정말로 비워졌는지, 단순 화면 전환이 아닌지)을 확인 완료.
- ✅ `playerLeft` 토스트 문구를 상황별로 분기: "상대방이 나가서 대기실로 돌아왔습니다"는 원래 게임 중 이탈(화면이 실제로 `game`→`room`으로 전환되는 경우)에만 맞는 문구인데, 로비(`RoomPage`)에 이미 같이 있던 중 상대가 나갔을 때도(화면 전환이 없는데) 똑같이 떴던 걸 분리함. `App.tsx`의 `playerLeft` 핸들러가 이벤트 수신 시점의 `screen.name`을 보고, `game`이면 기존 문구 + 화면 전환, `room`이면 화면 전환 없이 "상대방이 퇴장하였습니다."만 띄우도록 분기(이 두 갈래를 정확히 구분하려고 `useEffect` 의존성 배열에 `screen`을 추가해 항상 최신 화면 상태를 클로저로 잡게 함). 로비에서 나가기 버튼으로 나간 경우뿐 아니라 로비에서 단순 연결 종료로 나간 경우도 동일하게 짧은 문구로 통일됨(둘 다 "화면 전환이 필요 없는 이탈"이라는 같은 조건이라 자연스럽게 같이 처리됨). Playwright로 로비 이탈/게임 중 이탈 각각 올바른 문구가 뜨는지 검증 완료.
- ✅ 단판제 → 삼세판(3판 2선승제)으로 변경 + 승점 상시 표시. `Player`(백엔드 `room.types.ts`, 프론트 `types/index.ts`) 타입에 `wins: number` 추가(방 생성/참가 시 0으로 초기화). `room.service.ts`에 `WINS_TO_MATCH = 2` 상수와 `isMatchOver(room)` 헬퍼 추가, 기존 `finishRound`를 `applyRoundResult(roomCode, winner)`로 대체해 라운드 승자의 `wins`를 증가시키도록 함(무승부는 증가 없음 — 무승부 라운드는 승수 변동 없이 그냥 다음 라운드로 넘어감, 별도 처리 불필요). `markRematchReady`는 재경기 동의 시점에 `isMatchOver(room)`이 참이었으면(=매치가 끝난 뒤의 진짜 "재경기") 양쪽 `wins`를 0으로 리셋하고, 거짓이면(=아직 매치 진행 중, 그냥 "다음 라운드") 승점을 그대로 유지 — **새 이벤트를 만들지 않고 기존 `rematch`/`rematchStarted` 메커니즘을 그대로 재사용**해서 "다음 라운드"와 "매치 종료 후 재경기"를 한 흐름으로 처리함. `socket/index.ts`의 `selectHand` 핸들러가 라운드 판정 직후 `applyRoundResult` → 갱신된 승점(`scores: { socketId: wins }`)과 `matchOver` 여부를 담아 `result` 이벤트로 전송(`game/index.ts`의 `GameResult`에 `scores`/`matchOver` 필드 추가, 순수 가위바위보 판정은 `RoundJudgement`라는 별도 타입으로 분리해서 `judgeGame`은 그대로 두고 승점 계산은 소켓 핸들러 레벨에서 합성). `playersUpdated`가 매 라운드/재경기마다 이미 브로드캐스트되고 있었기 때문에 `Player.wins`를 필드에 추가하는 것만으로 프론트가 별도 동기화 코드 없이 상대방 승점까지 실시간으로 받아볼 수 있었음. `GamePage.tsx`: 방 코드 아래에 "나 N : M 상대" 점수 배지 + "3판 2선승" 안내를 상시 표시, 결과 화면 문구를 `matchOver` 여부로 분기(매치 안 끝났으면 "라운드 승리!/패배.../무승부!" + "다음 라운드" 버튼·"다음 라운드 동의" 배지, 매치 끝났으면 "게임 승리!/패배..." + "재경기" 버튼·"재경기 동의" 배지 — `matchOver`는 무승부 라운드에서는 절대 참이 될 수 없으므로 매치-종료 문구엔 무승부 케이스를 안 넣어도 됨). `CLAUDE.md`에 매치 규칙 설명과 `Player.wins`, `result` 페이로드 변경사항 반영. Playwright로 라운드 1승(점수만 오르고 "다음 라운드" 노출) → 라운드 2연승(매치 종료, "재경기"로 문구 전환) → 재경기 클릭(점수 0:0 리셋) 전체 흐름 검증 완료.
- ✅ 특수카드 시스템 도입(총🔫/중지🖕/거울🪞) + 카드 소모형 자원화. 기본카드(가위/바위/보)도 이번에 무제한 재사용에서 소모형으로 전환. 규칙(사용자 확정):
  - 특수카드는 기본카드(가위→총/바위→중지/보→거울)의 상위 호환. **일반 카드 상대**: 결정승이면 2승, 원래 무승부였을 베이스면 1승(무승부→승리 전환), 결정패면 상대 2승(자신 2패 취급). **특수끼리는** 그냥 원래 가위바위보 규칙(1승/무승부).
  - 획득: 매치(3판 2선승) 시작마다 기본 각 3장(총 9장) + 무작위 특수카드 1장 지급. 그 외엔 라운드 승리 시 +특수카드 1장, 패배 시 +특수카드 2장(전부 3종 중 무작위). "다음 라운드"(매치 안에서 이어가기)로는 재지급 없음.
  - 소모: 카드는 낼 때마다 소모, 진짜 무승부(특수의 무승부→승리 전환은 "진짜 무승부"가 아님)만 예외로 소모 안 됨. 무승부가 **3연속**이면 양쪽 카드를 매치 시작 상태(기본 각 3장 + 새 무작위 특수 1장)로 리셋(승점은 그대로) — 카드 고갈로 게임이 멈추는 걸 막는 안전장치.
  - 구현: `backend/src/game/rps/rps.types.ts`에 `BasicHand`/`SpecialHand`/`SPECIAL_BASE`/`isSpecialHand`/`baseOf` 추가해 `Hand`를 6종으로 확장. 신규 `backend/src/game/rps/rps.cards.ts`(`createEmptyCards`/`createStartingCards`/`grantCards`/`STARTING_BASIC_COUNT`/`CONSECUTIVE_DRAWS_TO_RESET`). `rps.game.ts`의 `judgeRps`가 `"A"|"B"|"draw"` 대신 `{ result, winsDelta }`를 반환하도록 재작성(특수/기본 조합별 매트릭스 그대로 구현). `game/index.ts`의 `RoundJudgement`/`GameResult`에 `winsDelta` 추가. `room.types.ts`의 `Player`에 `cards: Record<Hand, number>`, `Room`에 `consecutiveDraws: number` 추가. `room.service.ts`: `setReady`가 매치 최초 시작(`allReady`로 `PLAYING` 진입) 시 전원에게 `createStartingCards()` 지급, `selectHand`가 보유량 0인 카드 선택을 에러로 막음, `applyRoundResult(roomCode, winner, winsDelta)`로 시그니처 확장해 카드 소모/보상/연속무승부 카운터·리셋을 전부 처리, `markRematchReady`의 새 매치 분기에서 `cards`도 재지급, `removePlayer`가 남은 플레이어의 `cards`를 비움.
  - 프론트: `types/index.ts`에 `BasicHand`/`SpecialHand`/`Player.cards`/`GameResult.winsDelta` 추가. `game/rps/cards.ts`의 `CardDef`에 `category: "basic"|"special"` 추가하고 특수카드 3종 등록(라벨은 카드 폭 때문에 "중지손가락" 대신 "중지"로 축약). `HandCard.tsx`가 `category==="special"`이면 살구색 링으로 구분 표시(단 승패 결과 색이 있으면 그게 우선). `GamePage.tsx`의 손패 선택 영역을 고정 3장이 아니라 `self.cards`에서 보유량>0인 카드만(수량 배지 포함) 렌더링하도록 교체, `winsDelta===2`일 때 결과 문구에 "특수카드! 2승 획득" 강조 추가. `MainPage.tsx`의 임시 self Player 객체에도 6종 카드 0으로 채운 `cards` 필드 추가.
  - 🐛 구현 중 발견한 별개의 레이스 컨디션 버그(특수카드 없이도 항상 존재했던 문제, 이번에 카드 지급 타이밍 때문에 처음 겉으로 드러남): `ready` 이벤트 핸들러가 `playersUpdated`(갱신된 카드 포함)와 `gameStarted`를 연달아 emit하는데, `gameStarted`가 페이로드 없이 오다 보니 `RoomPage`가 리액트 state에 아직 반영 안 된 stale `players` 클로저를 그대로 `onGameStart(players)`에 넘기는 경쟁이 있었음(두 이벤트가 같은 틱에 도착하면 `setPlayers`가 아직 리렌더를 안 끝낸 상태). 그동안 `wins`는 매치 시작 시 항상 0이라 이 버그가 안 드러났을 뿐, 실제로는 계속 있던 버그. `gameStarted`도 `{ players }`를 실어 보내도록 고치고(`backend/src/socket/index.ts`), `RoomPage`가 클로저 대신 이 payload를 쓰도록 수정해서 근본적으로 해결(`CLAUDE.md` 이벤트 표에도 반영). 수정 전엔 매치 시작 직후 손패가 통째로 안 보이는 증상으로 나타났었음.
  - Playwright로 규칙 매트릭스 전 항목 검증: 실제로 배정된 무작위 특수카드의 베이스 타입을 스크립트가 읽어서 상대가 낼 카드를 역산 — 특수 결정승(2승 획득 문구까지 확인), 특수 결정패(상대 2승), 특수 vs 기본 무승부→1승 전환, 기본 라운드 카드 소모(3→2), 진짜 무승부 카드 미소모, 3연속 무승부 후 카드 리셋(2→3 복원 확인)까지 전부 통과.
- ✅ 특수카드 설명 추가(처음 보는 사람도 바로 이해하도록). `game/rps/cards.ts`의 `CardDef`에 `tag?: string` 추가해 특수카드 3종에 "가위/바위/보의 강화판" 짧은 태그를 달아 카드 밑에 항상 표시. 카드 자체엔 전체 규칙을 담기엔 공간이 부족해서, `GamePage.tsx`의 손패 선택 영역 위에 `<details>`/`<summary>`(네이티브 HTML, 터치·클릭 모두 자연스럽게 동작, 별도 JS 상태 불필요) 기반 "특수카드가 처음이신가요? 눌러서 규칙 보기" 접이식 안내를 추가 — 펼치면 강화판 대응 관계, 승리/무승부/패배별 승수, "특수카드끼리는 원래 규칙" 예외까지 요약해서 보여줌. 기본적으로 접힌 상태라 매 라운드 화면을 과도하게 차지하지 않으면서도 항상 같은 자리에서 발견 가능. Playwright로 접힌 상태 스크린샷과 `summary` 클릭 후 펼쳐진 상태, 카드 밑 태그 노출까지 확인.
- ✅ 대기실에서 경기 방식(3판 2선승/5판 3선승) 선택 가능하게 변경, 기본값을 5판 3선승으로. 기존엔 `room.service.ts`에 `WINS_TO_MATCH=2`가 상수로 박혀 있던 걸 `Room.winsToMatch`(방별 값, `room.types.ts`)로 옮기고 `DEFAULT_WINS_TO_MATCH=3`/`ALLOWED_WINS_TO_MATCH=[2,3]`로 관리, `isMatchOver`가 이 값을 참조하도록 변경. 신규 client→server 이벤트 `setMatchFormat`(`{roomCode, winsToMatch}`) → `room.service.setMatchFormat`이 유효성 검사 후 값 갱신 + 양쪽 `ready`를 리셋(방식이 바뀌면 다시 확인해야 하므로), `matchFormatUpdated`(`{winsToMatch}`)와 `playersUpdated`를 함께 브로드캐스트. 방을 생성/참가할 때도 현재 값을 알아야 해서 `roomCreated`/`playerJoined` 페이로드에 `winsToMatch`를 추가(`MainPage.tsx`가 받아서 `onEnterRoom`으로 전달, `App.tsx`의 `Screen` 타입 `room`/`game` 분기에 `winsToMatch` 필드 추가해 `RoomPage`→`GamePage`까지 threading). `RoomPage.tsx`에 "3판 2선승"/"5판 3선승" 버튼 두 개(현재 선택된 쪽이 primary 스타일)를 추가 — 어느 플레이어든 클릭 가능, 클릭 즉시 상대방 화면에도 실시간 반영되고 양쪽 Ready가 풀림. `GamePage.tsx`는 하드코딩했던 "3판 2선승" 문구를 `formatMatchLabel(winsToMatch)`(`${winsToMatch*2-1}판 ${winsToMatch}선승` 공식)로 교체해 실제 설정값을 그대로 보여줌.
  - `gameStarted`에도 `winsToMatch`를 함께 실어 보내도록 함(카드 지급 때 발견했던 것과 같은 클래스의 stale-closure 경쟁을 애초에 만들지 않기 위해 — `RoomPage`가 로컬 `winsToMatch` state를 클로저로 읽는 대신 이벤트 payload를 그대로 신뢰).
  - `CLAUDE.md`의 `Room`/`Player` 타입 문서(그동안 누락돼 있던 `consecutiveDraws`/`cards` 필드도 이 김에 보완), 매치 규칙 설명, Client→Server/Server→Client 이벤트 표 전부 갱신.
  - Playwright로 (1) 방 생성 시 기본값이 "5판 3선승"으로 선택돼 있는지 (2) 한쪽이 방식을 바꾸면 상대방 화면에 실시간 반영되고 이미 눌러둔 Ready가 풀리는지 (3) 실제로 "3판 2선승"으로 바꿔서 진행했을 때 1승 시점엔 안 끝나고 2승 시점에 정확히 매치가 끝나는지까지 검증 완료.
- ✅ 특수카드 보상 규칙 조정: 실제로 플레이해보니 승리 시에도 특수카드를 줘서 계속 특수카드를 손에 쥐고 있게 되던 문제 → **승리 시엔 특수카드를 주지 않고, 패배 시에만 1장**(기존 2장에서 축소) 주도록 변경. `room.service.ts`의 `applyRoundResult`에서 `grantCards(winner.cards, 1)` 호출을 제거하고 `grantCards(loser.cards, 2)` → `grantCards(loser.cards, 1)`로 수정(시작 지급/무승부 미소모/3연속 무승부 리셋 로직은 그대로). 인게임 규칙 안내(`GamePage.tsx`의 `<details>`)에는 애초에 보상 수량을 명시하지 않았어서(승/무/패별 승수 매트릭스만 설명) UI 문구 변경은 불필요했음. Playwright로 승자는 라운드 전후 특수카드 총량이 그대로, 패자만 정확히 +1 되는지 확인.
- ✅ 상대방 카드 소모 현황 공개(전략적 선택을 위해, 사용자 확정): 기본카드(가위/바위/보)는 상대방 것도 정확한 수량 그대로 보여주고, 특수카드(총/중지/거울)는 어떤 종류인지는 숨기되 **총 보유 개수만** 공개 — 상대가 뭘 이미 썼는지 보고 전략은 짤 수 있되, 특수카드의 "무슨 카드인지 모르는" 긴장감은 낼 때까지 유지. 이건 단순 프론트 표시 문제가 아니라 **서버가 애초에 상대방의 정확한 특수카드 구성을 클라이언트에 보내지 않도록** 막아야 진짜 의미가 있음(안 그러면 브라우저 개발자 도구 네트워크 탭에서 그냥 다 보임) — 그래서 `players`가 실리는 모든 이벤트(`playerJoined`/`playersUpdated`/`playerLeft`/`gameStarted`)를 방 전체 브로드캐스트 한 번에서 **수신자별 개별 emit**으로 바꿈. `backend/src/socket/index.ts`에 `toWirePlayers(players, viewerSocketId)` 헬퍼를 추가해 본인 항목은 원본 그대로, 상대방 항목은 `cards.gun/middleFinger/mirror`를 0으로 지우고 대신 `specialCardCount`(총 특수카드 개수, 서버가 계산)를 추가해서 반환하도록 함. 이 변환 결과를 `broadcastPlayers(io, room, event, extra?)` 헬퍼로 각 플레이어의 소켓에 개별 emit(`io.to(socketId).emit(...)`)하도록 기존 `io.to(roomCode).emit(...)` 호출들을 전부 교체. `frontend/src/types/index.ts`의 `Player`에 `specialCardCount: number` 추가, `GamePage.tsx` 헤더에 "{상대닉네임} 카드 ✊N ✌️N ✋N 🎴×N" 상시 표시 줄 추가(라운드마다 갱신되는 걸 계속 볼 수 있게 헤더에 고정 배치). Playwright로 상대가 라운드에서 진 뒤 그 카드 수량은 정확히 -1, 특수카드 총량은 +1 되는 게 실시간으로 보이는지 확인.
- ✅ 기본카드 시작 수량 3장 → **2장**으로 축소(`game/rps/rps.cards.ts`의 `STARTING_BASIC_COUNT`). 위에서 추가한 상대 카드 공개 기능과 맞물려서, 기본카드 폭이 좁아진 만큼 상대가 뭘 이미 썼는지 보고 남은 선택지를 좁혀나가는 전략성이 더 커짐(수학적으로 여전히 매치 도중 카드가 0장까지 갈 수 없다는 이전 불변식도 그대로 유지 — 승리 시 소모 최대치는 `winsToMatch`(2~3)뿐이라 시작 수량 5장(기본 2×2+특수 1)보다 항상 작음).
- ✅ 3연속 무승부로 카드가 초기화될 때 화면에 안내 문구 추가(그동안은 서버에서 조용히 리셋만 되고 클라이언트엔 아무 표시가 없었음). `room.service.ts`의 `applyRoundResult` 반환 타입을 `Room` 단독에서 `{ room, cardsReset: boolean }`로 바꿔서 이번 라운드가 리셋을 유발했는지 호출부에 알려주도록 함. `socket/index.ts`가 이 값을 `GameResult.cardsReset`에 실어 `result` 이벤트로 전송(`game/index.ts`의 `GameResult`, `frontend/src/types/index.ts`의 `GameResult`에 각각 필드 추가). `GamePage.tsx` 결과 화면에 `result.cardsReset`이 참이면 안내 배지를 라운드 결과 문구 밑에 표시. `CLAUDE.md`의 `result` 이벤트 페이로드 표에도 `winsDelta`/`cardsReset` 반영(그동안 누락돼 있던 `winsDelta`도 이 김에 보완). Playwright로 (1) 시작 기본카드가 실제로 2장인지 (2) 결정 라운드로 먼저 카드를 하나 소모시킨 뒤 (3) 같은 카드로 3연속 무승부를 유도해서 리셋 안내가 뜨는 동시에 소모됐던 카드 수량이 정확히 원상복구(2장)되는지까지 확인.
- ✅ 무승부도 카드를 소모하게 변경 + 리셋 트리거를 "3연속 무승부" → "누적 3회 무승부"로 바꾸고, 무승부 스택을 화면에 상시 표시(사용자와 대화로 설계 확정: 무승부가 공짜였던 걸 없애 예측 가능한 패가 상대와 겹치면 손해를 보게 만들어 전략성을 높이되, "연속"이 아니라 "누적"으로 바꿔서 무승부가 띄엄띄엄 나도 반드시 3번째에 안전장치가 걸리도록 함 — 계산상 리셋 사이 최악의 소모량이 항상 시작 보유량 이내라 카드 고갈 걱정 없음).
  - `game/rps/rps.cards.ts`: `CONSECUTIVE_DRAWS_TO_RESET` → `DRAWS_TO_RESET`으로 개명(값은 3 그대로, 의미만 "누적"으로 바뀜).
  - `room.types.ts`의 `Room.consecutiveDraws` → `Room.drawStack`으로 개명.
  - `room.service.ts`의 `applyRoundResult`: 카드 소모를 무승부/결정 라운드 분기 이전으로 옮겨서 **양쪽 다 낸 카드를 항상 소모**하도록 변경(기존엔 무승부면 소모를 건너뜀). 무승부 분기에서 `drawStack`을 증가시키고 임계값 도달 시 카드 리셋 + `drawStack=0`. 결정 라운드 분기에서는 더 이상 `drawStack`을 0으로 되돌리지 않음(누적 유지가 핵심 변경점). `markRematchReady`가 새 매치를 시작하는 분기에서 `drawStack`도 0으로 초기화하도록 추가(예전엔 결정 라운드가 항상 카운터를 0으로 되돌려서 매치가 끝날 때 자동으로 0이었지만, 누적 방식에서는 더 이상 그렇지 않아 명시적으로 리셋해줘야 함).
  - `socket/index.ts`: `gameStarted`에 `drawStack` 추가, `result`(`GameResult`)에 `drawStack` 추가, 그동안 페이로드 없이 보내던 `rematchStarted`에도 `{ drawStack }`을 실어 보내도록 변경(새 매치 시작 시 0으로 리셋된 값을 프론트가 클로저 추측 없이 바로 받도록 — `gameStarted` 때 썼던 것과 같은 패턴).
  - `frontend/src/types/index.ts`의 `GameResult`에 `drawStack: number` 추가. `GamePage.tsx`에 `drawStack` 로컬 상태 추가(초기값 0, `result`/`rematchStarted` 이벤트로 갱신), 헤더에 "무승부 스택 ●●○" 점 3개짜리 인디케이터 상시 표시(채워진 개수 = 현재 누적 횟수). 리셋 안내 문구를 "3연속 무승부!" → "무승부 {N}회 누적!"으로 수정. 특수카드 규칙 안내(`<details>`)에도 "카드는 무승부여도 소모되고, N번 쌓이면 초기화" 한 줄 추가.
  - `CLAUDE.md`의 `Room` 타입, 매치 규칙 설명, `result`/`gameStarted`/`rematchStarted` 이벤트 표를 전부 갱신(겸사겸사 이벤트 표 중간에 설명 문단이 끼어들어 테이블이 두 조각으로 쪼개져 렌더링되던 기존 포맷 문제도 정리).
  - Playwright로 (1) 무승부 라운드에서 실제로 카드가 1장 소모되는지 (2) 결정 라운드가 껴도 스택이 리셋되지 않고 그대로 유지되는지(연속이 아니라 누적임을 증명) (3) 스택이 2까지 쌓였다가 3번째 무승부에서 리셋 안내와 함께 카드가 원상복구되는지까지 확인. 헤더의 점 인디케이터가 라운드마다 정확히 채워지는 것도 스크린샷으로 확인.
- ✅ 같은 공유기 내 다른 기기(LAN)와 실제로 플레이 테스트할 수 있도록 설정 정리. 예전엔 `docker-compose.yml`에 `VITE_BACKEND_URL=http://localhost:3000`, `CLIENT_ORIGIN=http://localhost:5173`이 하드코딩돼 있어서, 호스트가 아닌 다른 기기에서 접속하면 그 기기 자신의 localhost를 찾다가 실패(CORS/연결 실패)했음 — 사용자가 "예전에 테스트가 안 됐다"고 기억하던 원인. `docker-compose.yml`을 `${VITE_BACKEND_URL:-http://localhost:3000}` / `${CLIENT_ORIGIN:-http://localhost:5173}` 형태로 바꿔 기본값(솔로 개발 시 localhost)은 그대로 유지하면서, gitignore된 루트 `.env` 파일(이 PC 전용, git엔 안 올라감)에 실제 LAN IP를 넣어 오버라이드하도록 함. 호스트 PC의 이더넷 어댑터 IP를 `Get-NetIPConfiguration`으로 확인(`175.195.8.97`, 서브넷 175.195.8.0/24, Windows 네트워크 프로필 Private — 방화벽 이슈 가능성 낮음 확인). 호스트도 `localhost` 대신 같은 LAN IP로 접속해야 두 기기의 Origin이 동일해져서 CORS가 안 걸림. Playwright로 `http://175.195.8.97:5173`을 통해 실제로 소켓 연결·방 생성이 되는지 확인 완료, 이후 사용자가 실제 모바일 기기로 접속해서 정상 동작 확인함(모바일 반응형 레이아웃도 문제없이 잘 나옴). 원격지(다른 네트워크)에서 테스트하려면 ngrok 같은 터널링이 별도로 필요하다는 점은 안내만 하고 아직 설정 안 함.
- ✅ README.md를 초기 MVP 상태에서 현재 상태로 전면 갱신 — 실제 플레이 화면 스크린샷 5장(`docs/screenshots/`, Playwright로 촬영: 메인/대기실/카드선택/특수카드 규칙/결과), 현재 게임 규칙 전체(매치 방식 선택, 특수카드 매트릭스, 카드 소모/보상/무승부 스택 리셋), 실제 소스 구조를 반영한 프로젝트 트리 추가.
- ✅ 가위/바위/보 표시 순서를 한국어 관용 순서(**가위→바위→보**)로 통일 — 지금까지 `frontend/src/game/rps/cards.ts`의 `HAND_CARDS`가 영어식(rock→scissors→paper) 순서였고, `MainPage.tsx` 타이틀 이모지(✊✌️✋)와 `GamePage.tsx`의 상대 카드 현황 줄도 같은 순서를 따르고 있었음 — 전부 가위(✌️)→바위(✊)→보(✋) 순으로 재정렬. 특수카드 순서(총→중지→거울, `GamePage.tsx`의 `<details>` 안내 이모지 🔫🖕🪞)는 이미 대응 순서가 맞아서 손 안 댐. `CLAUDE.md`의 `Hand` 타입 설명에 "화면에 손 모양을 나열할 땐 항상 가위→바위→보 순서를 따른다"는 규칙을 명시해서 앞으로 추가되는 UI도 이 순서를 기본으로 따르도록 함(사용자가 앞으로도 이 순서를 계속 쓰겠다고 확정). Playwright로 메인 화면 타이틀, 손패 선택 그리드, 상대 카드 현황 줄 전부 가위-바위-보 순서로 뜨는지 확인.
- ✅ 개발 호스트를 PC → **라즈베리파이**로 이전, 외부망(다른 네트워크)에서도 접속 가능하도록 배포. 라즈베리파이가 통신사로부터 공인 IP(`175.195.8.67`)를 NAT 없이 인터페이스에 직접 할당받는 환경임을 `hostname -I`와 외부 조회(`curl ifconfig.me`) 결과가 동일함으로 확인 — 별도 포트포워딩 없이 곧바로 외부 접속 가능. 루트 `.env`(gitignore, 라즈베리파이 전용)의 `VITE_BACKEND_URL`/`CLIENT_ORIGIN`을 이 IP로 갱신(기존엔 PC의 공인 IP `175.195.8.97`이 잘못 들어있었음 — PC와 라즈베리파이는 같은 통신사 대역이지만 서로 다른 공인 IP를 각각 받는 구조였음, `curl ifconfig.me`로 PC 쪽도 별도 확인). 라즈베리파이가 공인 IP를 직접 물고 있어 보안 노출 위험이 커서, `sudo ss -tulnp`로 열린 포트 확인 후(`ufw` 미설치 상태에서 22/3000/5173이 전부 방화벽 없이 열려있었음) `ufw` 설치, 22(SSH)/3000(backend)/5173(frontend)만 허용하고 나머지 인바운드 차단(`default deny incoming`)하도록 설정. 추가로 `fail2ban` 설치해 SSH 브루트포스 자동 차단 활성화(`sshd` jail 정상 동작 확인, SSH는 이미 키 기반 인증 사용 중). 모바일 데이터(다른 네트워크)로 실제 접속·플레이까지 정상 동작 확인 완료. 도메인 연결(A 레코드)과 랜선 교체 시 IP 변동 가능성은 논의만 하고 아직 실행하지 않음.
- ✅ 라즈베리파이가 오래 켜져 있으면 간헐적으로 SSH/웹 접속이 끊기는 문제 원인 파악: `vcgencmd get_throttled` 결과가 `0x50000`(bit16 부팅 후 저전압 발생 이력 + bit18 스로틀링 발생 이력)으로 나와, 범용 USB 충전기의 저전압으로 인해 네트워크 컨트롤러가 불안정해지는 것으로 확인(공인 IP는 안 바뀌어 있어 DHCP 변경은 원인에서 제외, 케이스 통풍/방열판이 있어 과열도 배제). 케이블 재연결로 우선 복구. 재발 방지를 위해 사용자가 라즈베리파이의 `docker-compose.yml`에 직접 `restart: unless-stopped`를 frontend/backend 양쪽에 추가(컨테이너가 죽어도 자동 재시작)해뒀던 것을 PC 저장소에도 동일하게 반영(다른 필드는 파이/PC 완전 동일, 이 두 줄만 diff였음 — 로컬 개발 워크플로우엔 영향 없음을 확인 후 적용). 근본 해결책(정품 전원 어댑터 교체)은 사용자가 진행할 예정, 아직 검증 전.
- ✅ 대기실/게임 진행 중 채팅 기능 구현(제한/스팸 방지 포함). 채팅은 특정 게임(rps) 전용이 아니라 방 공통 기능이라 `room/`·`game/rps/`를 건드리지 않고 신규 `backend/src/chat/chat.service.ts`로 분리(socket.io에 의존하지 않는 순수 검증 모듈). 사용자가 명시적으로 요청한 제한: 메시지 길이 200자 초과 거부, 소켓당 최소 전송 간격 500ms(쿨다운), 동일 메시지 연속 3회째 도배 차단, 빈 문자열/제어문자(널바이트 등) 거부 — 전부 `validateAndRecordMessage(socketId, raw)` 하나로 순서대로 검증하고 위반 시 각각 다른 한국어 메시지로 `Error` throw(기존 `room.service.ts` 스타일과 동일), 위반은 기존 `error` 이벤트로 재사용(페이지마다 이미 리스너가 있어 새 배관 불필요). "SQL 인젝션 같은 특수문자도 막고 싶다"는 요청엔 이 프로젝트가 DB를 아예 안 써서 SQL 인젝션 자체가 해당 없고, XSS도 프론트가 `dangerouslySetInnerHTML` 없이 순수 JSX로만 렌더링해 React가 자동 이스케이프하므로(코드베이스 전체 검색으로 미사용 확인) 별도 이스케이프가 불필요하다는 점을 설명하고, 대신 제어문자 필터링만 추가함. 소켓 레벨 방어로 `server.ts`의 `new Server(...)`에 `maxHttpBufferSize: 1_000_000`(1MB) 명시(기본값과 동일하지만 의도를 코드로 남김). `room.service.ts`에는 채팅 핸들러가 "이 소켓이 정말 이 방의 플레이어인가"를 검증하며 닉네임도 얻을 수 있는 `findPlayerInRoom(roomCode, socketId)` export만 추가. `leaveCurrentRoom`에서 `chatService.clearChatState(socket.id)` 호출해 소켓별 rate-limit 상태 메모리 누수 방지.
  - 프론트: `types/index.ts`에 `ChatMessage` 타입 추가, 공유 UI 레이어에 `components/Chat/ChatBox.tsx`(메시지 목록 + `maxLength=200` input + 글자수 카운터 + 전송) 신규 작성. `App.tsx`가 최상위에서 `chatMessage` 이벤트를 한 번만 구독해 `messages` state를 관리(기존 `playerLeft` 토스트와 같은 위치/패턴), `RoomPage`/`GamePage`에 `messages`/`onSendMessage` props로 내려줘서 대기실→게임 화면 전환에도 채팅 기록이 그대로 유지됨(방 나가기 시엔 초기화).
  - Docker가 이 세션 동안 계속 안 떠서(Docker Desktop 미기동), 로컬에 이미 설치돼 있던 `node_modules`로 백엔드(`tsx`)/프론트(`vite`)를 대체 포트(3100/5273)에 직접 띄워 검증. `playwright-core`를 저장소 루트에 임시 설치(`--no-save`, 테스트 후 삭제)하고 이전 세션이 남겨둔 로컬 캐시 Chromium(`ms-playwright/chromium-1228`)을 재사용해 두 브라우저 컨텍스트로 (1) 정상 메시지 송수신 (2) 대기실→게임 화면 전환 후에도 채팅 기록 유지 (3) 클라이언트 `maxLength`를 DOM 조작으로 우회해도 서버가 200자 초과를 거부 (4) 500ms 이내 연속 전송 시 쿨다운 에러 (5) 동일 메시지 3회 연속 시 도배 차단 — 5개 전부 통과 확인. `tsc -b`(frontend)/`tsc --noEmit`(backend)/`oxlint`/`vite build` 전부 통과.
  - `CLAUDE.md` Client→Server/Server→Client 이벤트 표에 `chatMessage` 추가, "채팅 제한 규칙" 절 신설.
- ✅ 채팅창이 메시지가 쌓여 스크롤이 생겨도 새 메시지 도착 시 자동으로 하단까지 스크롤되도록 수정(`ChatBox.tsx`에 `listRef` + `messages` 변경 시 `scrollTop = scrollHeight` 적용). 이전엔 직접 스크롤을 내려야만 새 메시지가 보였음. Playwright로 메시지 15개 연속 전송 후 발신자/수신자 양쪽 다 스크롤이 하단에 붙어있는지 확인.
- ✅ URL로 방 초대 가능하게 변경 + 닉네임 자동/사전 설정. 백엔드는 변경 없음(`room.service.ts`의 `joinRoom`이 이미 "존재하지 않는 방"/"방이 가득 찼습니다" 에러를 던지고 있어서 URL로 들어온 잘못된 초대도 기존 메커니즘으로 처리됨). 화면이 몇 개 안 되는 프로젝트 규모라 `react-router-dom` 없이 `App.tsx`가 `window.location.pathname`을 `/^\/room\/(\d{6})$/`로 파싱하는 수동 라우팅으로 구현(Vite dev 서버가 SPA fallback을 기본 지원해 `/room/123456` 직접 접속도 별도 서버 설정 불필요).
  - 신규 `frontend/src/nickname.ts`: `localStorage`(`jmg_nickname`)에 닉네임을 저장/재사용(재방문 시 재입력 불필요, 사용자 확정), 저장된 값이 없으면 `손님{4자리 랜덤 숫자}` 임시 닉네임을 자동 생성해 채움.
  - 신규 `frontend/src/pages/JoinInvitePage.tsx`: URL 초대 전용 화면(방 생성/코드 입력 UI 없이 닉네임 확인 후 바로 "입장하기"). `MainPage.tsx`의 기존 `joinRoom`/`error` 이벤트 구독 패턴을 그대로 재사용. 방이 없거나 가득 찼을 때도 기존 에러 문구 + "메인으로" 탈출 버튼으로 처리.
  - `MainPage.tsx`: 닉네임 입력 초기값을 빈 문자열에서 `loadNickname()`으로 변경(임시 닉네임이 미리 채워짐, 수정 가능), 생성/참가 성공 시 `saveNickname()` 호출.
  - `App.tsx`: `Screen`에 `invite` 분기 추가, 마운트 시 URL을 파싱해 초기 화면 결정. 방 생성/참가/초대 입장 시 `enterRoom()` 헬퍼가 `history.pushState`로 주소를 `/room/{code}`로 갱신, 나가기 시 `/`로 복원. 뒤로가기(`popstate`)로 화면을 복원하는 로직은 의도적으로 넣지 않음(새로고침 시 세션이 사라지는 기존 구조와 일관성 유지, URL은 순수 공유용).
  - `RoomPage.tsx`: 방 코드 복사 버튼이 이제 6자리 코드가 아니라 전체 초대 URL(`origin + /room/{code}`)을 클립보드에 복사하도록 변경(기존 HTTP 환경 폴백 로직은 그대로 재사용), 안내 문구를 "탭해서 초대 링크 복사"로 수정.
  - Docker 대신 로컬 dev 서버(포트 3100/5273)로 Playwright 검증: (1) 방 생성 시 주소가 `/room/<code>`로 바뀌는지 (2) 복사 버튼이 전체 URL을 복사하는지 (3) 그 URL을 새 브라우저 컨텍스트로 직접 열면 닉네임이 임시값(`손님####`)으로 미리 채워진 확인 화면이 뜨는지 (4) 거기서 입장하면 정상적으로 대기실에 합류하는지 (5) 존재하지 않는 코드로 직접 URL 접속 시 에러 + "메인으로" 버튼으로 탈출되는지 (6) 닉네임을 바꾸고 새로고침해도 localStorage로 유지되는지 — 총 10개 항목 전부 통과. `tsc -b`/`oxlint`/`vite build` 통과.
  - `CLAUDE.md`에 "방 초대 URL / 닉네임" 절 신설, 게임 진행 플로우 다이어그램에 URL 초대 경로 추가.
- ✅ 접속 남용 방지(연결/방 생성 속도 제한) — "DoS처럼 과하게 접속하면 자동 차단되는지" 질문에서 시작. 확인해보니 `ufw`(포트 필터링)/`fail2ban`(SSH 브루트포스만) 둘 다 애플리케이션 레벨 남용은 못 막고 있었음(같은 IP가 소켓 연결을 무한 반복하거나 `createRoom`을 무한 호출해도 그대로 받아줌). 대규모 분산 DoS(DDoS)는 앱 코드로 막을 영역이 아니라 범위에서 제외하고, 사용자가 확정한 애플리케이션 레벨 두 가지만 적용:
  - 신규 `backend/src/security/rateLimiter.ts`: "키(IP)당 시간창 내 N회"를 재는 범용 슬라이딩 윈도우 리미터(`createRateLimiter(maxAttempts, windowMs)`). 채팅 쿨다운(`chat.service.ts`)은 메시지 내용 검증까지 얽혀있어 그대로 두고 별도 분리.
  - `socket/index.ts`: `io.use` 미들웨어로 연결 자체를 IP당 분당 20회로 제한(초과 시 핸드셰이크 거부, 클라이언트엔 `connect_error`), `createRoom`/`joinRoom` 핸들러 진입을 IP당 분당 10회로 제한(초과 시 기존 `error` 이벤트로 안내).
  - `App.tsx`: `connect_error` 이벤트를 구독해 기존 토스트 UI로 사유를 표시(연결 자체가 거부되는 드문 경우도 조용히 실패하지 않도록).
  - IP는 `socket.handshake.address` 기준 — 현재 구성(Docker 브리지 네트워크, 리버스 프록시 없음)에서는 DNAT가 목적지만 바꾸고 출발지 IP는 보존해서 정확함. 나중에 nginx 등을 앞에 두면 `X-Forwarded-For` 신뢰 설정이 별도로 필요하다는 점을 `CLAUDE.md`에 메모해둠.
  - 로컬 dev 서버(포트 3100) + socket.io-client로 직접 Node 스크립트 작성해 검증: (1) 동일 IP로 21번째 연결 시도부터 `connect_error`("너무 많은 연결 시도입니다") 발생 (2) 같은 소켓으로 `createRoom`을 11번째 호출부터 `error`("너무 많은 요청입니다") 발생, 1~10번째는 정상 `roomCreated` — 둘 다 통과. 디버깅 중 테스트 스크립트가 처음엔 통과 못 했는데, 원인은 리미터 로직이 아니라 이전 세션에서 백그라운드로 띄워둔 구버전 백엔드 프로세스가 포트를 물고 있어 코드 변경이 반영 안 된 것이었음(`netstat`/`taskkill`로 정리 후 재검증). `tsc --noEmit`(backend)/`tsc -b`/`oxlint`/`vite build`(frontend) 전부 통과.
  - `CLAUDE.md`에 "연결/요청 제한" 절 신설.
- ✅ 같은 방에 동일 닉네임이 들어오면 자동으로 구분자 부여. "같은 닉네임이 한 방에 있으면 어떻게 되는지" 질문에서 시작 — 확인해보니 중복 검사가 전혀 없었고(서버는 `socketId`로만 구분해서 당장 게임 로직은 안 깨졌지만), 나중에 3인 이상 게임을 추가하면 화면에 동명이인이 여럿 뜨는 혼란이 생길 수 있어 미리 처리. `room.service.ts`에 `dedupeNickname(existingNicknames, nickname)` 추가 — 이미 있는 닉네임이면 `(2)`부터 시작해 비어있는 번호를 찾을 때까지 증가시키는 방식(예: "(2)"도 이미 있으면 "(3)"으로), 인원이 몇 명이든 그대로 동작하도록 설계. `joinRoom`에서 새 플레이어를 방에 추가하기 직전에 이 함수로 최종 닉네임을 계산해서 사용(`createRoom`은 항상 빈 방의 첫 플레이어라 충돌 자체가 불가능해 손 안 댐). 프론트엔드는 변경 없음 — `playerJoined` 이벤트의 `players` 배열에 이미 최종(구분자 붙은) 닉네임이 실려오므로 자동 반영됨. socket.io-client로 로컬 dev 서버에 직접 검증: 같은 닉네임("테스트")으로 두 번째 참가 시 "테스트 (2)"가 되는지 확인 완료. `tsc --noEmit` 통과. `CLAUDE.md`의 `Player` 설명에 한 줄 추가.
- ✅ **[실험적, 정식 기능 아님]** 가위바위보를 Phaser 3로 이식한 품질 테스트 페이지(`/phaser-test`) 추가. 다음 게임(2D 액션/아케이드, 캐릭터 이동/스프라이트 애니메이션 필요)에 Phaser 엔진을 도입할지 미리 가늠해보기 위한 프로토타입 — 사용자가 범위를 "기존 `GamePage`는 절대 안 건드리고, 같은 socket 이벤트를 쓰는 별도 페이지로 병렬 구축"으로 확정. `npm install phaser`(frontend, `^4.2.1` 설치됨 — Phaser 4는 API가 기존에 알려진 Phaser 3와 거의 동일해서 `Scene`/`GameObjectFactory`/`Tweens` 등 마이그레이션 이슈 없음, 다만 `SceneManager`에 `getScene()` 같은 헬퍼는 없어서 `game.scene.keys["씬이름"]`으로 인스턴스를 직접 가져와야 함을 타입 선언 파일 확인으로 알아냄).
  - 신규 `frontend/src/game/rps/phaser/PlayZoneScene.ts`: 카드 아트 에셋 없이 이모지(`add.text`)+사각형(`add.rectangle`)만으로 카드 그리기, 보유 카드 선택 그리드 + 대결 슬롯 + 뒤집기 연출(스케일X 0→복귀 트윈)까지 한 Scene에서 처리. `syncState(state)` 하나로 매번 전체 다시 그리는 방식(카드 개수가 적어서 성능 문제 없음, React 쪽에서 상태 동기화 로직을 단순하게 유지하기 위한 선택).
  - 신규 `frontend/src/game/rps/components/PhaserPlayZone.tsx`: React ↔ Phaser 브릿지. 마운트 시 1회 `Phaser.Game` 생성/언마운트 시 `destroy(true)`, 이후 props 변경마다 별도 `useEffect`가 `scene.syncState(...)` 호출해서 동기화(Phaser엔 리액트식 리렌더가 없어 명령형으로 밀어넣는 표준 패턴).
  - 신규 `frontend/src/pages/PhaserGamePage.tsx`: 기존 `GamePage.tsx`의 socket 구독/승점/무승부스택/상대카드현황/채팅 로직을 그대로 재사용하고, `<PlayZone>` + 손패 선택 그리드 부분만 `<PhaserPlayZone>`으로 교체(주변 UI는 품질 테스트 대상이 아니라 그대로 둠).
  - `App.tsx`: 마운트 시 `window.location.pathname === "/phaser-test"`를 1회 캡처하는 `usePhaserRenderer` 플래그만 추가(기존 초대 링크 캡처와 동일 패턴, 이후 `pushState`로 주소가 바뀌어도 값 유지), `screen.name === "game"`일 때 이 플래그로 `GamePage`/`PhaserGamePage`를 분기. `MainPage`/`RoomPage`/`GamePage`는 한 줄도 안 건드림. `/phaser-test`는 `MainPage`에 노출 안 하는 URL 전용 진입점 — 초대 링크(`/room/{code}`)는 항상 일반 모드로 열리므로, 둘이 같이 테스트하려면 양쪽 다 `/phaser-test`로 들어가서 방 코드를 수동 입력해야 함(정식 기능이 아니라 이 한계는 그대로 둠).
  - Docker로 배포할 땐 `phaser`가 새 의존성이라 다음엔 `docker compose up --build`가 필요함(기존 Tailwind 추가 때와 같은 케이스, PC/파이 양쪽 다 마찬가지).
  - Docker Desktop이 이번에도 계속 꺼져있어서 로컬 dev 서버(포트 3100/5273)로 Playwright 검증: 두 브라우저 컨텍스트로 `/phaser-test` 접속 → 방 생성/수동 코드 참가 → 양쪽 Ready → Phaser 캔버스가 마운트되는지 → 캔버스 안 카드를 실제로 클릭하면 `selectHand`가 나가서 상대 대기 문구가 뜨는지 → 양쪽 다 선택 후 `result` 수신 시 결과 패널(승/패/무 + 재경기 버튼)이 뜨는지 → 콘솔/페이지 에러 없는지, 전부 통과. 스크린샷으로 실제 카드 뒤집기/테두리 색(승/패/무) 렌더링을 사용자가 직접 볼 수 있게 확인 — 선택 그리드가 사라진 자리에 빈 공간이 남는 등 레이아웃은 아직 거친 1차 프로토타입 수준(품질 검증이 목적이라 다듬지 않음). `tsc -b`/`oxlint`/`vite build` 통과(Phaser 때문에 번들 크기 500KB 경고 발생 — 예상된 트레이드오프, 이 실험 라우트만의 문제라 code-splitting 등은 하지 않음).
  - 마음에 안 들면 이 5개 파일(`PlayZoneScene.ts`, `PhaserPlayZone.tsx`, `PhaserGamePage.tsx`, `App.tsx`의 분기, `phaser` 의존성)만 지우면 기존 게임엔 전혀 영향 없음.
- ✅ 버그 수정: `/phaser-test`에서 방을 만들고 게임까지 가면 Phaser 대신 원래 카드 UI가 나오는 문제. 원인은 Phaser 모드 플래그를 경로(`/phaser-test`) 하나로만 판단했는데, 방을 만들면 `enterRoom()`이 주소를 `/room/{code}`로 바꿔버려서(초대 링크 기능) `/phaser-test` 자체가 주소에서 사라지고, 그 뒤 어떤 이유로든(Vite 개발 서버 리로드, 브라우저 새로고침 등) 페이지가 다시 로드되면 플래그가 재계산되면서 꺼져버렸음. `App.tsx`에 `isPhaserRequested()`(최초 진입은 `/phaser-test` 경로 또는 `?engine=phaser` 쿼리 둘 다 인정)와 `withPhaserFlag(path, on)`(pushState 할 때마다 쿼리를 계속 붙여줌) 헬퍼를 추가해 `enterRoom`/`handleExit`/초대 취소 핸들러가 모두 이 쿼리를 유지하도록 수정 — 이제 `/room/{code}?engine=phaser`처럼 유지되어 페이지가 다시 로드돼도 플래그가 안 꺼짐. 단, 초대 링크 복사 버튼(`RoomPage`)은 원래부터 쿼리 없는 `origin + /room/{code}`만 복사하므로, 공유받은 상대는 여전히 일반 모드로 열림(의도한 동작, 안 바뀜). Playwright로 (1) 방 생성 후 주소에 `engine=phaser` 쿼리가 유지되는지 (2) 정상 플로우로 게임 화면까지 갔을 때 Phaser가 렌더링되는지 (3) 상대가 남아있는 상태로 방 만든 사람이 새로고침 → 초대 화면으로 돌아가 재입장 → 그래도 Phaser로 게임이 시작되는지까지 확인.
- ✅ 실제 파이 배포 후 사용자 피드백 반영 — `/phaser-test` 레이아웃 3차 개선(연속 3개 커밋):
  1. 상대/나 라벨이 서로 반대 슬롯에 붙어있던 버그(라벨 y좌표와 슬롯 y좌표가 뒤바뀜) + 상대 슬롯이 캔버스 상단 밖으로 살짝 잘려나가던 좌표 버그 수정, 캔버스/카드 크기 확대(420×660, 슬롯 카드 110×160).
  2. 화면을 한 번 더 키움(480×750, 슬롯 카드 130×190) + 페이지를 `max-w-5xl`로 넓혀 채팅을 오른쪽 사이드로 이동, 게임 영역과 나란히 한 화면에 들어오도록 변경(`ChatBox`에 `messageListClassName` prop 추가해 이 페이지에서만 채팅 목록을 더 길게 표시).
  3. 카드 이모지/텍스트 상단이 잘려 보이는 문제 — Phaser의 잘 알려진 이슈(Text 객체의 캔버스 텍스처 크기를 폰트 메트릭으로 추정하는데 일부 글리프의 실제 렌더링 높이가 더 커서 상단이 잘림)를 이 씬의 모든 `add.text` 호출에 `padding: { top: 10, bottom: 6 }`를 추가해 해결. 동시에 라운드/게임 종료 시에만 보이던 결과 패널(아바타+버튼)을 요청대로 채팅 위로 옮기면서, 사용자가 추가로 원한 대로 상시 패널로 확장 — 헤더에 있던 스코어/상대 카드 현황/무승부 스택을 제거하고 오른쪽 채팅 위 패널에 닉네임(항상)+스코어(항상)+상대 카드 현황(항상)+무승부 스택(항상)+결과 시에만 뜨는 승패 문구/카드리셋안내/Ready배지/버튼을 하나로 통합.
    - 구현 중 발견한 별개 버그: 선택 그리드의 카드 수량 배지(`badge`/`badgeText`)가 카드 위치(`x,y`)를 반영 안 하고 캔버스 원점 근처에 잘못 생성되고 있었음(월드 좌표로 만든 뒤 `(0,0)` 컨테이너에 추가하면서 카드와 무관한 위치에 남는 버그) — `box`(카드 컨테이너) 자식으로 옮겨서 수정, 배지가 이제 각 카드 우상단에 정확히 표시됨.
    - 카드 클릭 판정용 히트 영역도 Container 자식 대신 씬 최상위 독립 객체로 분리(원인 조사 중 발견: Playwright 헤드리스 자동화의 CDP 합성 마우스 이벤트가 Phaser의 PointerEvent 입력과 간헐적으로 안 맞아떨어지는 테스트 도구 특유의 이슈였고, 실제 사용자의 진짜 마우스 조작과는 무관함을 별도 스크립트로 확인 — 그래도 Container 중첩을 줄이는 게 더 안전한 패턴이라 그대로 반영).
    - React StrictMode(dev)가 `PhaserPlayZone`의 Phaser.Game 생성 effect를 mount→cleanup→mount로 두 번 실행하면서, 첫 번째(곧 destroy될) 게임의 비동기 READY 콜백이 늦게 도착해 `sceneRef.current`를 죽은 씬으로 덮어쓸 수 있는 경쟁 상태를 발견 — `cancelled` 플래그로 방지(표준 React 패턴).
  - Playwright + 스크린샷으로 매 단계 시각 확인, `tsc -b`/`oxlint`/`vite build` 전부 통과.
- ✅ `/phaser-test` 모바일 반응형 크기 + 캔버스 위 스크롤 통과. 데스크톱 기준으로 계속 키워온 고정 픽셀 캔버스(480×750)가 모바일 뷰포트보다 넓어서 화면을 넘치고, Phaser가 터치 이벤트에 기본으로 `preventDefault`를 걸어서 캔버스 위에서 스와이프해도 페이지 스크롤이 아예 안 되던 문제. `frontend/src/game/rps/components/PhaserPlayZone.tsx`만 수정(`PlayZoneScene.ts`의 좌표 계산은 그대로 둠):
  - Phaser `Game` config에 `scale: { mode: Phaser.Scale.FIT, autoCenter: Phaser.Scale.CENTER_BOTH, width: SCENE_WIDTH, height: SCENE_HEIGHT }` 추가 — 내부 게임 좌표계(480×750)는 그대로 두고 화면에 그려지는 크기만 부모 컨테이너에 맞춰 비율 유지하며 축소.
  - `input: { touch: { capture: false } }` 추가 + 컨테이너에 `touchAction: "pan-y"` — 터치 스와이프가 더 이상 캔버스에 막히지 않고 페이지 스크롤로 통과됨(카드 선택은 탭이라 영향 없음).
  - 컨테이너 폭을 `width: "100%"`(퍼센트 기준)로 뒀더니 모바일에서도 안 줄어드는 버그 발견 — 원인은 부모(`Card`)가 flex 자식이라 퍼센트 기준이 되는 폭 자체가 불확정적이었던 것(플렉스박스의 흔한 함정). `width: min(${SCENE_WIDTH}px, calc(100vw - 7rem))`처럼 뷰포트(vw) 기준으로 직접 계산하는 방식으로 바꿔서 부모의 flex 사이징과 무관하게 항상 뷰포트 안에 들어오도록 수정.
  - Playwright로 모바일 크기 뷰포트(390×844) 컨텍스트를 만들어 (1) 가로 스크롤/넘침 없는지 (2) 캔버스가 실제로 뷰포트에 맞게 축소되는지(480→278px) (3) 캔버스 영역에서 스크롤 제스처 시 페이지가 실제로 스크롤되는지(스크린샷으로 패널/채팅이 보이는지 확인) (4) 카드 탭은 여전히 되는지 (5) 데스크톱 뷰포트에서는 480px 그대로 유지되는지(회귀 없음) 전부 통과. `tsc -b`/`oxlint`/`vite build` 통과.
- ✅ `/phaser-test` 카드 선택을 "탭=하이라이트, 확인 버튼=확정" 2단계로 변경. 모바일 스크롤 통과(`touch.capture:false`) 부작용으로, 카드 위에서 스크롤하려고 손가락을 대면 그 즉시 `pointerdown`이 발화해 카드가 바로 서버로 확정 전송되던 문제 — "Phaser를 모바일에서 쓰기 불편하면 아예 기존 UI로 돌아갈까, 아니면 고쳐볼까" 논의 중 사용자가 직접 제안한 해결책(탭/스크롤 구분 로직 대신, 탭은 그냥 하이라이트만 하고 별도 "확인" 버튼을 눌러야 실제 확정되게 해서 실수 커밋 자체를 막음)으로 확정.
  - `PlayZoneScene.ts`: `PlayZoneSyncState`의 `onSelect`를 `onHighlight`로 개명(의미 변경: 확정이 아니라 하이라이트만 알림), `highlightedHand` 필드 추가. 선택 그리드에서 하이라이트된 카드는 전용 테두리 색(primary `0x7c6fd6`)과 살짝 확대로 표시.
  - `PhaserPlayZone.tsx`: `onSelectHand` prop을 `onHighlight`로 교체, `highlightedHand`를 그대로 씬에 전달.
  - `PhaserGamePage.tsx`: 새 state `highlightedHand`(하이라이트만, 서버로 아직 안 나감) 추가, 기존 `handleSelectHand`(실제 확정 + 서버 emit)는 그대로 두되 새 "확인" 버튼(React `Button`, 하이라이트 없으면 비활성)에서만 호출하도록 변경. `rematchStarted` 시 하이라이트도 초기화.
  - Playwright로 모바일 뷰포트에서 (1) 확인 버튼이 하이라이트 전엔 비활성인지 (2) 카드를 탭해도 아직 서버로 안 나가고(대기 문구 없음) 확인 버튼만 활성화되는지 (3) 확인을 눌러야 실제로 확정되어 상대 대기 문구가 뜨는지 (4) 양쪽 다 확인 후 결과가 정상 수신되는지 전부 통과. `tsc -b`/`oxlint`/`vite build` 통과.
- ✅ `/phaser-test` 품질 테스트 결과를 정식 채택 — 사용자가 "phaser로 만든 것을 기본으로 하자"로 확정, `GamePage`를 Phaser 버전으로 교체.
  - `frontend/src/pages/GamePage.tsx`: 기존 내용을 `PhaserGamePage.tsx` 내용으로 통째로 교체(타이틀에서 "(Phaser 테스트)" 문구만 제거), `PhaserGamePage.tsx`는 삭제.
  - 이제 쓰이지 않게 된 구버전 비-Phaser 카드 컴포넌트 `frontend/src/game/rps/components/PlayZone.tsx`, `HandCard.tsx` 삭제(다른 곳에서 참조 없음 확인 후 삭제).
  - `App.tsx`: `usePhaserRenderer`/`isPhaserRequested`/`withPhaserFlag` 엔진 분기 로직 전부 제거(더 이상 두 버전을 병행하지 않으므로), `pushState` 경로도 쿼리 없는 순수 경로로 단순화. `screen.name === "game"`일 때 무조건 `GamePage`(Phaser 버전) 렌더링.
  - `CLAUDE.md`: 기술 스택에 Phaser 추가, 프로젝트 구조에 `game/rps/phaser`·`components/PhaserPlayZone.tsx` 경로 추가, "Phaser 엔진 도입 검토(실험적)" 절을 "Phaser 엔진(정식 채택)"으로 갱신.
  - `tsc -b`/`oxlint`/`vite build` 전부 통과(번들 크기 500KB 초과 경고는 기존에 알려진 트레이드오프로 유지).
- ✅ 게임 선택 메커니즘 추가 — 사용자가 다음 게임(룰 미정, **4인용** 확정)을 준비하며 "가위바위보와 새 게임을 어떻게 고르게 할지" 질문. 확인 결과 (1) 선택은 방 만들기 전 `MainPage`에서, (2) 초대링크로 들어온 상대는 선택 없이 방의 `gameType`을 그대로 따름, 으로 확정. 조사해보니 `room.service.ts`가 `MAX_PLAYERS=2` 상수 하드코딩 + `createRoom`이 `gameType`을 인자로 안 받고 항상 `"rps"` 고정이라, `CLAUDE.md`가 표방하던 "새 게임 추가 시 room/socket 안 건드리기" 원칙이 실제로는 지켜지지 않고 있었음 — 이번에 그 뼈대를 실제로 갖춤.
  - 신규 `backend/src/game/registry.ts`: `GAME_DEFS`(`{ [gameType]: { maxPlayers } }`) — 실제 구현된 게임만 등록, 현재는 `rps: { maxPlayers: 2 }` 하나.
  - `backend/src/room/room.types.ts`: `Room`에 `maxPlayers: number` 필드 추가.
  - `backend/src/room/room.service.ts`: `MAX_PLAYERS` 상수 제거, `createRoom(nickname, gameType, socketId)`가 `GAME_DEFS[gameType]`을 조회해 없으면 에러를 던지고 있으면 `room.maxPlayers`에 저장. `joinRoom`/`allReady`/`allHandsSelected`의 정원 비교를 전부 `room.maxPlayers` 기준으로 교체.
  - `backend/src/socket/index.ts`: `createRoom` 핸들러가 `{ nickname, gameType }`을 받아 try/catch로 감싸(잘못된 gameType이면 기존 `error` 이벤트로 안내) `roomService.createRoom`을 호출. `roomCreated`/`playerJoined`/`gameStarted` payload에 `gameType`/`maxPlayers` 추가.
  - 신규 `frontend/src/game/registry.ts`: `GAME_OPTIONS` — `{ id: "rps", label: "가위바위보", maxPlayers: 2 }`와, 아직 실존하지 않는 두 번째 게임을 위한 `{ id: "coming-soon-4p", label: "4인 게임", maxPlayers: 4, comingSoon: true }`(비활성 표시 전용, 백엔드로 전송 안 됨).
  - `frontend/src/pages/MainPage.tsx`: `GAME_OPTIONS`를 순회하는 선택 타일 UI 추가(기본값 `"rps"`, `comingSoon` 항목은 클릭 불가+"준비 중" 배지), `createRoom` emit에 `gameType` 포함, `roomCreated`/`playerJoined` 핸들러가 응답의 `gameType`/`maxPlayers`를 그대로 `onEnterRoom`에 전달.
  - `frontend/src/pages/JoinInvitePage.tsx`/`App.tsx`: `onJoined`/`onEnterRoom`/`Screen`(`"room"`/`"game"` variant)이 `gameType`/`maxPlayers`를 함께 들고 다니도록 시그니처 확장.
  - `frontend/src/pages/RoomPage.tsx`: 새 prop `gameType`/`maxPlayers` 수신, `players.length < 2`/`=== 2` 하드코딩을 `maxPlayers` 기준으로 일반화("N/M명" 표시로 변경), 경기 방식(`MATCH_FORMATS`) 선택 UI는 `gameType === "rps"`일 때만 렌더링(RPS만의 "N판 M선승" 개념이라 다른 게임엔 안 맞을 수 있어 대비, 현재는 rps만 있어 동작 변화 없음).
  - `GamePage.tsx`는 건드리지 않음 — 두 번째 게임이 아직 실존하지 않아 분기(dispatch) 대상이 없음(YAGNI).
  - 검증: `backend`/`frontend` 모두 `tsc` 통과, 프론트 `oxlint`/`vite build` 통과. Docker Desktop이 꺼져 있어(`docker ps` 실패) 로컬 dev 서버(백엔드 3100, 프론트 5173)로 socket.io-client 스크립트를 직접 작성해 (1) `createRoom`에 `gameType: "rps"`를 보내면 `roomCreated`에 `gameType`/`maxPlayers: 2`가 정확히 오는지 (2) 두 번째 플레이어 참가 시 `playerJoined`에도 동일하게 오는지 (3) 정원(2명) 초과 시 세 번째 참가자가 "방이 가득 찼습니다" 에러를 받는지 (4) 존재하지 않는 `gameType`으로 방 생성 시도 시 "지원하지 않는 게임 타입입니다" 에러를 받는지 — 전부 통과 확인. 다만 이 세션엔 브라우저 자동화 도구가 연결되어 있지 않아 `MainPage`의 실제 선택 타일 UI(비활성 "4인 게임" 배지 등)는 화면으로 직접 확인하지 못했음 — 사용자가 `http://localhost:5173`에서 직접 확인 필요.
  - `CLAUDE.md`: `Room` 인터페이스에 `maxPlayers` 추가, Socket 이벤트 표(`createRoom`/`roomCreated`/`playerJoined`/`gameStarted`) 갱신, "게임 선택 방식" 절 신설(선택 시점, 레지스트리 패턴, 새 게임 추가 절차, 아직 일반화 안 된 부분(`cards`/`wins`/`applyRoundResult`의 RPS 전용 로직)을 후속 과제로 명시).
- ✅ `MainPage` 타이틀을 "가위바위보 온라인"에서 "JoinMyGame"으로 변경, 기존 타이틀 위에 있던 가위바위보 이모지(✌️✊✋)는 게임 선택 타일 중 "가위바위보" 버튼 아래 여백(라벨만 있고 `comingSoon` 배지가 없어 "4인 게임" 타일보다 짧아 보이던 자리)으로 이동. `frontend/src/game/registry.ts`의 `GameOption`에 `icon?: string` 필드 추가, `rps` 항목에 `icon: "✌️✊✋"` 설정 — `MainPage.tsx`가 타일 렌더링 시 라벨 아래 아이콘을 표시. `tsc -b`/`oxlint` 통과.
- ✅ 가위바위보에 AI(컴퓨터) 상대 추가 — "혼자서도 즐길 수 있게" 요청. 처음엔 "AI와 대전" 전용 버튼으로 방 생성 시점부터 AI를 넣는 방식을 검토했으나, 사용자가 "기존 대기방에서 AI를 추가하는 방식"을 제안해 대기실(`RoomPage`)에서 빈 자리를 채우는 토글 버튼 방식으로 확정(추가로 "제거도 필요하다"는 요청을 받아 add/remove 토글로 마무리). AI 손 선택 전략은 "무작위, 단 특수카드 보유 시 우선 사용"으로 확정(상대의 `specialCardCount`가 이미 화면에 보이므로 예측 가능하되 완전 랜덤보다 도전적인 밸런스라고 판단).
  - 핵심 설계 포인트: `room.service.ts`에서 `p.ready = false`로 전원을 리셋하던 세 곳(`applyRoundResult`, `markRematchReady`의 새 라운드 진입, `setMatchFormat`)을 전부 `p.ready = !!p.isAI`로 바꿔 "AI는 항상 ready" 불변식을 심음 — 덕분에 `ready`/`rematch`/`setMatchFormat` 소켓 핸들러는 AI를 전혀 의식할 필요가 없어짐(사람이 한 번만 액션해도 AI 몫까지 즉시 반영됨).
  - `backend/src/game/registry.ts`: `GameDef`에 `supportsAI?: boolean` 추가, `rps: { maxPlayers: 2, supportsAI: true }`.
  - 신규 `backend/src/game/rps/rps.ai.ts`: `chooseAiHand(cards)` — 보유 특수카드(총/중지/거울)가 있으면 그중 무작위 우선, 없으면 기본 카드 중 무작위.
  - `backend/src/room/room.types.ts`: `Player`에 `isAI?: boolean` 추가.
  - `backend/src/room/room.service.ts`: `addAiPlayer(roomCode)`(정원 초과/AI 미지원 게임이면 에러, 남은 빈 자리를 전부 `ai:${roomCode}:${seq}` socketId를 가진 AI 플레이어로 채움) / `removeAiPlayer(roomCode)`(`isAI` 필터로 제거) 신설. `removePlayer`에 `room.players = room.players.filter(p => !p.isAI)` 한 줄 추가해 사람이 나가면 남은 AI도 같이 정리되어 기존 "정원 0이면 방 삭제" 분기가 그대로 재사용되게 함.
  - `backend/src/socket/index.ts`: `addAiPlayer`/`removeAiPlayer` 핸들러 추가(응답은 기존 `playersUpdated` 재사용). `selectHand` 핸들러가 사람의 선택 직후 `isAI && selectedHand === null`인 플레이어를 찾아 `chooseAiHand`로 즉시 대신 선택해줌. `broadcastPlayers`가 AI 뷰어(실제 연결된 소켓 없음)에는 emit하지 않도록 필터링.
  - `frontend/src/types/index.ts`/`frontend/src/game/registry.ts`: `Player.isAI?`/`GameOption.supportsAI?` 추가.
  - `frontend/src/pages/RoomPage.tsx`: 정원 미달+AI 지원 게임이면 "AI로 채우기", AI가 이미 있으면 "AI 제거" 버튼을 토글로 표시(둘 다 새 리스너 없이 기존 `playersUpdated` 구독으로 반영). 상대가 AI면 방 코드 공유 카드를 숨김(정원이 이미 찼으니 초대해도 에러만 뜸).
  - `frontend/src/pages/GamePage.tsx`: 상대가 AI면 `ChatBox`를 숨김(응답 없는 봇에게 채팅은 의미 없음).
  - `MainPage.tsx`/`App.tsx`/`createRoom`/`roomCreated` 관련 코드는 전혀 안 건드림 — AI는 방이 만들어진 뒤 대기실에서만 추가/제거됨.
  - 검증: 백엔드/프론트 `tsc` 통과, 프론트 `oxlint`/`vite build` 통과. 로컬 dev 서버(백엔드 3100, 프론트 5173 — 백엔드는 `tsx watch`가 아니라 `tsx` 1회 실행이라 코드 변경 후 재시작 필요했음, `EADDRINUSE`로 이전 프로세스가 안 죽어있던 걸 `netstat`/`taskkill`로 정리)에 socket.io-client 스크립트로 (1) `addAiPlayer` 후 `playersUpdated`에 사람+AI 2명, AI `ready: true` (2) 정원 찬 상태에서 재요청 시 "방이 가득 찼습니다" 에러 (3) `removeAiPlayer` 후 1명으로 줄고 재추가 가능 (4) `ready` 한 번만으로 `gameStarted` 즉시 발생 (5) `selectHand` 한 번만으로 `result` 즉시 수신 + 여러 라운드 돌려 AI가 실제로 특수카드(거울 등)를 우선 내는지 확인 (6) `rematch` 한 번만으로 `rematchStarted` 즉시 발생 (7) `leaveRoom` 후 같은 코드로 재입장 시도하면 "존재하지 않는 방입니다"(방 완전 정리) — 전부 통과. 테스트 스크립트 자체에 버그가 두 번 있었음(카드가 바닥나는 손만 반복해서 무한 대기, `Promise.race`를 emit보다 늦게 구성해 응답을 못 받고 무한 대기) — 둘 다 로직 문제가 아니라 테스트 스크립트 버그였고 고쳐서 재검증함. 브라우저 자동화 도구가 이번 세션에 없어 "AI로 채우기/제거" 버튼 UI 자체는 육안으로 보지 못함.
  - `CLAUDE.md`: `Player`에 `isAI` 필드 문서화, Socket 이벤트 표에 `addAiPlayer`/`removeAiPlayer` 추가, "AI 대전" 절 신설(진입 방식, "AI는 항상 ready" 불변식, 손 선택 전략, 방 정리 규칙).
- ✅ 두 번째 게임 구현: **알까기 배틀로얄**(`alkkagi`, 4인용). 처음엔 "방향키로 계속 움직이는 실시간 게임"으로 설계를 시작했다가, 사용자가 "마우스로 방향·힘을 조준(당구 스타일 드래그) → 전원 동시 발사 → 밀려나면 탈락 → 살아남은 사람만 재조준을 반복 → 최후 1인 승리"로 룰을 크게 정정해 그 방향으로 다시 설계함. 이 정정 덕분에 RPS와 같은 "라운드마다 전원이 동시에 행동 제출 → 서버가 한 번에 판정" 구조를 그대로 따르게 되어, 상시 tick 루프나 고빈도 이동 이벤트 없이 **판정이 필요한 순간에만 물리 시뮬레이션을 동기적으로 한 번 계산**하는 방식으로 단순화됨(RPS의 `judgeGame`이 동기 함수인 것과 같은 성격).
  - `backend/src/game/registry.ts`에 `alkkagi: { maxPlayers: 4, supportsAI: true }` 등록. `game/index.ts`의 `judgeGame` 스위치엔 케이스를 넣지 않음(1:1 손 비교 계약이 안 맞음) — 대신 신규 `backend/src/game/alkkagi/`(`alkkagi.types.ts`/`alkkagi.physics.ts`/`alkkagi.ai.ts`/`alkkagi.round.ts`)로 완전히 별도 처리.
  - 물리(`alkkagi.physics.ts`): Matter.js 없이 순수 벡터 연산(원-원 충돌 displacement, 마찰 감속, 반경 이탈 판정). `simulateRound(inputs, arenaRadius)`가 모든 말이 멈추거나 `MAX_STEPS`(300)에 도달할 때까지 내부 루프를 돌며 매 스텝의 위치를 `keyframes` 배열에 기록해 통째로 반환 — 클라이언트는 이 리플레이를 로컬에서 재생만 함.
  - 라운드 진행(`alkkagi.round.ts`): `initializeAlkkagiMatch`(원형 배치 + 무대 반경 세팅 + AI 조준 선채움), `fillAiAims`(AI는 라운드 시작 즉시 조준을 미리 채워둠 — 사람만 기다리면 됨), `submitAim`/`allAimsSubmitted`, `resolveRound`(살아있는 전원의 조준을 동시 발사 → 결과 반영 → 무대 축소 → 생존 1명 이하면 매치 종료 판정까지 한 번에 처리, `winnerId: null`은 동시 탈락 무승부).
  - 타입(`room.types.ts`): RPS 필드(`selectedHand`/`cards`/`wins`/`drawStack`/`winsToMatch`)는 그대로 두고, `Player.alkkagi?: AlkkagiState`(x/y/alive/aim), `Room.alkkagiArena?: { radius, round }`를 네임스페이스로 묶어 옵셔널 추가(RPS처럼 필드를 평탄하게 늘어놓지 않음).
  - 소켓(`socket/index.ts`): `alkkagiAim { roomCode, dx, dy, power }`(제출 1회, 저빈도 — 별도 rate limiter 불필요) → `finishAlkkagiRoundIfReady` 헬퍼가 `allAlkkagiAimsSubmitted`를 확인해 판정하고 `alkkagiRoundResult { keyframes, arenaRadius, round, matchOver, winnerId }`를 emit. 이 헬퍼는 **while 루프**로 되어 있어서, 사람이 전원 탈락하고 AI끼리만 남아도(양쪽 다 조준이 이미 채워져 있으므로) 매치가 끝날 때까지 자동으로 라운드를 이어서 판정함 — 그렇지 않으면 방이 영원히 멈춰있게 됨.
  - 매치 중 접속 끊김 처리: `room.service.ts`의 `removePlayer`에 분기 추가 — `gameType==="alkkagi" && gameState==="PLAYING"`이면 기존 RPS 동작(방 전체를 대기실로 되돌림)을 타지 않고 그 자리에서 `alkkagi.alive=false`로 탈락 처리만 하고 나머지 인원끼리 게임 계속(4인용이라 한 명이 나갔다고 남은 인원의 매치를 날릴 수 없음). `leaveCurrentRoom`이 이 경우 `finishAlkkagiRoundIfReady`도 함께 호출해, 탈락 처리 때문에 나머지 전원의 조준이 채워진 상태가 되면 즉시 판정이 이어지도록 함.
  - 프론트(`frontend/src/game/alkkagi/`): `phaser/AlkkagiScene.ts`(원형 무대 렌더링, 포인터 드래그로 조준선 미리보기 → 릴리즈 시 `onAim(dx,dy,power)`, `playResult(keyframes, arenaRadiusAfter, onComplete)`로 서버가 보내준 리플레이를 `update()` 루프에서 순차 재생), `components/PhaserAlkkagiArena.tsx`(RPS `PhaserPlayZone`과 동일한 wrapper/mount 분리·Game 생성 패턴 재사용, 다만 터치 캡처 비활성화(`touch.capture:false`)는 이 게임엔 안 맞아서 안 가져옴 — 드래그 조준이라 기본 터치 캡처가 오히려 맞음). 신규 `frontend/src/pages/AlkkagiGamePage.tsx`가 `ChatBox`/`PlayerAvatar`/`Card`/`Button` 등 기존 공유 UI를 그대로 재사용. `App.tsx`가 `screen.gameType`으로 `GamePage`/`AlkkagiGamePage`를 분기.
  - 검증 중 발견해 고친 버그 2건(둘 다 로컬 브라우저 수동 테스트로 발견 — 백엔드 단위/소켓 스크립트로는 안 드러났었음):
    1. **재경기 버튼이 매치 종료 즉시 비활성화되던 버그**: `alkkagi.round.ts`의 `resolveRound`가 `player.ready`를 전혀 리셋하지 않아서, 로비에서 Ready 누를 때 걸어둔 `ready=true`가 매치 끝까지 그대로 남아있었음(RPS의 `applyRoundResult`는 매 라운드 `p.ready=!!p.isAI`로 리셋하는데, 알까기는 이 리셋 로직 자체가 없었음) → "재경기" 버튼이 `disabled={self?.ready}`라서 매치가 끝나자마자 이미 눌린 것처럼 비활성화되어 사람이 다시 누를 방법이 없었음. `resolveRound`에도 동일한 리셋을 추가해서 해결.
    2. **사람이 탈락한 뒤 AI끼리 자동으로 여러 라운드가 이어질 때 리플레이 애니메이션이 건너뛰어지던 버그**: `finishAlkkagiRoundIfReady`의 while 루프가 `alkkagiRoundResult`를 짧은 시간 안에 여러 번 emit할 수 있는데, 프론트가 이걸 단일 `roundResult` state 하나로만 받다 보니 뒤에 도착한 이벤트가 앞의 애니메이션이 재생되기도 전에 덮어써버림(리액트 state가 배칭되지 않는 별개 소켓 이벤트라 매번 새로 렌더 → effect가 그때마다 `scene.playResult`를 다시 호출해 이전 재생을 끊고 새로 시작). `AlkkagiGamePage.tsx`를 큐(`queue`) 기반으로 재구성 — 도착한 결과를 큐에 쌓아두고, 재생 중(`current`)이 아닐 때만 큐에서 하나씩 꺼내 순서대로 재생하도록 수정.
  - Claude in Chrome(브라우저 자동화 확장)으로 로컬 dev 서버(백엔드 3100, 프론트 5273)에 직접 접속해 실제 화면으로 검증: (1) 메인 화면에서 "알까기 배틀로얄" 타일 선택 → 방 생성 시 정원 4명으로 뜨는지 (2) 대기실 "AI로 채우기"로 3자리 채운 뒤 Ready 한 번으로 게임 시작, 4명이 원형으로 균등 배치되는지 (3) 내 말을 드래그해서 조준 → 릴리즈 시 실제로 발사되고 다른 플레이어들과 충돌/무대 이탈 판정이 애니메이션으로 재생되는지 (4) 결정승(우승자 있음)/무승부(동시 탈락) 양쪽 결과 화면이 올바로 뜨는지 (5) 재경기 클릭 시 위치/무대 반경/생존 상태가 전부 초기화된 새 매치로 리셋되는지 — 위 두 버그를 이 과정에서 발견해 고친 뒤 전부 정상 동작 확인. `tsc -b`(frontend)/`tsc --noEmit`(backend)/`oxlint`/`vite build` 전부 통과. 백엔드 물리/라운드 판정 로직은 브라우저 테스트 이전에 헤드리스 `tsx` 스크립트(물리 단위 검증)와 socket.io-client 스크립트(AI 3+실제 소켓 1개로 매치 전체 진행, 매치 중 접속 끊김 시나리오 포함)로 먼저 검증한 뒤 UI를 올렸음(둘 다 검증 후 삭제된 임시 스크립트).
  - `frontend/src/game/registry.ts`의 `coming-soon-4p` 자리를 `alkkagi` 항목(`icon: "🔴🔵"`)으로 교체 완료 — 이제 메인 화면에서 실제로 선택 가능.
  - 튜닝 필요(현재 모두 placeholder 상수, 실제 플레이해보며 조정 필요): `alkkagi.physics.ts`의 `POWER_TO_SPEED`(18)가 무대 시작 반경(220) 대비 다소 강해서, AI끼리도 랜덤 조준만으로 1~2라운드 안에 매치가 끝나는 경우가 잦음(체감상 좀 빠름) — 다음에 만질 때 `POWER_TO_SPEED`를 낮추거나 `ARENA_START_RADIUS`를 키우는 쪽으로 조정 고려.
  - 범위 밖(이번엔 안 함): 모바일 터치 조준 검증(드래그 자체는 될 가능성이 높지만 데스크톱 마우스로만 확인함), 매치 중 접속 끊김 후 진짜 재접속/복귀(세션이 없어서 지금은 그냥 그 자리에서 탈락 처리만 됨 — 앱 전체에 재접속 개념이 아직 없다는 기존 한계와 동일).

## 2. 현재 진행 중인 작업

- (없음)

## 3. 앞으로 해야 할 작업

- ⬜ 알까기 물리 상수(발사 속도/무대 크기/축소 속도/밀치기 반경 등) 실제 플레이해보며 튜닝 — 현재는 매치가 다소 빨리 끝나는 편.
- ⬜ (필요 시) MVP 이후 추가 확장: 묵찌빠, 오목, 카드게임 등

## 4. 현재 이슈나 메모

- Windows 호스트 + Docker 조합에서 `.dockerignore` 없이 `COPY . .`를 하면 호스트에서 설치한 `node_modules`(Windows용 바이너리)가 컨테이너의 Linux용 `node_modules`를 덮어써 `tsx` 실행이 `node.exe: not found`로 실패하는 문제가 있었음. `.dockerignore`로 `node_modules` 제외 + 오염된 익명 볼륨은 `docker compose down -v`로 초기화하여 해결.
- 익명 볼륨(`/app/node_modules`)은 `docker compose up`으로 재생성해도 이전 컨테이너의 내용을 이어받는 경우가 있으므로, 의존성 관련 이슈 발생 시 `docker compose down -v` 후 재빌드할 것.
- backend/frontend 모두 로컬 검증용으로 호스트에도 `npm install`이 되어 있음 (`.gitignore`/`.dockerignore`로 제외되므로 컨테이너 빌드에는 영향 없음).
- (구) Ready 상태는 서버가 상대방의 ready 여부를 별도로 브로드캐스트하지 않았음 — `playersUpdated` 이벤트 추가로 해결됨(위 항목 참고). 이제 `RoomPage`/`GamePage` 모두 서버가 보내주는 `players[].ready`를 그대로 신뢰해서 상대방 상태를 실시간으로 보여줌.
- 컨테이너는 현재 실행 중 (`docker compose up -d` 상태) — 필요시 `docker compose down`.
- 루트 `.env`(gitignore됨, 라즈베리파이 전용)에 `VITE_BACKEND_URL=http://175.195.8.67:3000`, `CLIENT_ORIGIN=http://175.195.8.67:5173`이 들어있음 — 이 IP는 이제 PC가 아니라 **라즈베리파이의 공인 IP**(NAT 없이 인터페이스에 직접 할당됨). 나중에 순수 로컬(솔로) 개발로 돌아가려면 `.env`를 지우거나 값을 비우면 `docker-compose.yml`의 기본값(`localhost`)으로 돌아감. IP는 DHCP라 재부팅/랜선 교체 시 바뀔 수 있음 — 라즈베리파이에서 `hostname -I`/`curl ifconfig.me`로 재확인 후 `.env` 갱신할 것.
- 라즈베리파이는 `ufw`(22/3000/5173만 허용, 나머지 인바운드 차단)와 `fail2ban`(SSH 브루트포스 차단, `sshd` jail)이 설치·활성화된 상태. 향후 방화벽 규칙을 바꾸거나 새 포트를 열 때는 `sudo ufw status verbose`로 현재 상태를 먼저 확인할 것.
- 도메인 구입 시 A 레코드를 라즈베리파이 공인 IP로 연결하면 도메인 접속이 가능해지지만, IP가 고정이 아니라면 DDNS가 필요할 수 있고 `.env`의 URL도 도메인으로 갱신해야 함 — 아직 미실행, 필요 시 진행.
- gh CLI가 로컬에 없었어서 winget으로 설치 후 `gh auth login`으로 로그인, `gh repo create --source=. --push`로 저장소 생성과 첫 푸시를 한 번에 진행함.
- Windows 바인드 마운트에서는 `tsx watch`(backend)와 Vite HMR(frontend) 모두 파일 변경 감지를 놓치는 경우가 있었음 — 코드 수정 후 브라우저/API에 반영이 안 되면 `docker compose restart <service>`로 강제 재시작할 것. (`index.html`처럼 Vite가 매 요청마다 디스크에서 직접 읽는 정적 파일은 재시작 없이도 즉시 반영됨.)
